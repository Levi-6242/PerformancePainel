# Dependências do dashboard O&M (app.py).
# Versões VERIFICADAS no Python 3.14.6 / Windows (todas com wheel cp314 disponível).
# Em Python 3.11–3.13 também instalam. Pins = o que está rodando na máquina de origem.
flask==3.1.3
flask-compress==1.24
requests==2.34.2
pandas==3.0.3
numpy==2.4.6
openpyxl==3.1.5
matplotlib==3.10.9
psycopg2-binary==2.9.12
pycognito==2024.5.1
python-dotenv==1.2.2
waitress==3.0.2          # servidor WSGI de produção no Windows (usado pelo app.py)
gunicorn==23.0.0         # SÓ Linux/Railway (Procfile); instala no Windows mas NÃO roda (usa fcntl)
